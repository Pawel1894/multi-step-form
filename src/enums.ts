export enum STEPS {
  STEP_1 = 1,
  STEP_2 = 2,
  STEP_3 = 3,
  STEP_4 = 4,
}

export enum ERRORS {
  REQUIRED = "This field is required",
  NUMBERS = "This field can't contain numbers",
  EMAIL = "Not a valid email address",
  INVALID_TEL = "Not a valid phone number",
}
